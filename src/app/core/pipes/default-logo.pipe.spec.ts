import { DefaultLogoPipe } from './default-logo.pipe';

describe('DefaultLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
